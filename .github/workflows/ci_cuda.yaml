name: CI / cuda

on:
  workflow_dispatch:

jobs:
  test-cuda:
    name: CI / cuda (${{ matrix.name }})

    # This lane requires a self-hosted ARM64 CUDA/NCCL GPU runner.
    # Keep it manual so normal PRs are not blocked when that runner is unavailable.
    if: ${{ github.event_name == 'workflow_dispatch' }}

    concurrency:
      group: ${{ github.workflow }}-${{ matrix.name }}-${{ github.head_ref || github.run_id }}
      cancel-in-progress: true

    runs-on: [self-hosted, Linux, ARM64, gpu, cuda]

    strategy:
      matrix:
        include:
          - name: cuda
            features: cuda,nccl
          # - name: cuda+nccl
          #   features: cuda,nccl

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl build-essential libssl-dev protobuf-compiler pkg-config

      - name: Install Rust Stable
        uses: dtolnay/rust-toolchain@stable

      - name: CUDA sanity
        run: |
          nvidia-smi || true
          nvcc --version || true

      - name: Clippy
        run: cargo clippy --features ${{ matrix.features }}

      - name: Test
        run: cargo test --features ${{ matrix.features }}
